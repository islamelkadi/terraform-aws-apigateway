# Checkov configuration for terraform-aws-apigateway
# Suppressions for checks that are not applicable to example/demo code


skip-check:
  # KMS IAM Policy - Intentional for flexible key policies in examples
  - id: CKV_AWS_111
    comment: "KMS module uses flexible IAM policies for demonstration purposes. Users should customize based on their security requirements."
  - id: CKV_AWS_356
    comment: "KMS module uses wildcard resources for flexibility in examples. Users should restrict in production."
  - id: CKV_AWS_109
    comment: "KMS module IAM policies are intentionally permissive for examples. Users should apply least privilege in production."
  
  # Terraform Module Sources - Using semantic versioning tags
  - id: CKV_TF_1
    comment: "Using semantic version tags (?ref=v1.0.0) instead of commit hashes for better maintainability and readability."
  - id: CKV_TF_2
    comment: "Using semantic version tags (?ref=v1.0.0) which is a valid versioning strategy for stable releases."
  
  # API Gateway Caching - Optional feature, adds cost
  - id: CKV_AWS_120
    comment: "API Gateway caching is optional and adds cost. Users should enable based on their performance and cost requirements."
  - id: CKV_AWS_225
    comment: "API Gateway method caching is optional and adds cost. Users should enable based on their use case."
  
  # API Gateway Client Certificate - Optional security feature
  - id: CKV2_AWS_51
    comment: "Client certificate authentication is optional and adds complexity. Users should enable based on their security requirements."
  
  # API Gateway Request Validation - Use-case specific
  - id: CKV2_AWS_53
    comment: "Request validation requires defining request models which are use-case specific. Users should implement based on their API requirements."
  
  # API Gateway WAF - Optional security feature, adds cost
  - id: CKV2_AWS_29
    comment: "WAF protection adds significant cost and complexity. Users should enable based on their security and budget requirements."
