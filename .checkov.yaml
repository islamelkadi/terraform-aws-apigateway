# Checkov configuration for terraform-aws-apigateway
# Suppressions for checks that are not applicable to example/demo code

skip-check:
  # KMS IAM Policy - Intentional for flexible key policies in examples
  - CKV_AWS_111  # KMS module uses flexible IAM policies for demonstration purposes
  - CKV_AWS_356  # KMS module uses wildcard resources for flexibility in examples
  - CKV_AWS_109  # KMS module IAM policies are intentionally permissive for examples
  
  # Terraform Module Sources - Using semantic versioning tags
  - CKV_TF_1  # Using semantic version tags (?ref=v1.0.0) instead of commit hashes
  - CKV_TF_2  # Using semantic version tags (?ref=v1.0.0) which is a valid versioning strategy
  
  # API Gateway Caching - Optional feature, adds cost
  - CKV_AWS_120  # API Gateway caching is optional and adds cost
  - CKV_AWS_225  # API Gateway method caching is optional and adds cost
  
  # API Gateway Client Certificate - Optional security feature
  - CKV2_AWS_51  # Client certificate authentication is optional and adds complexity
  
  # API Gateway Request Validation - Use-case specific
  - CKV2_AWS_53  # Request validation requires defining request models which are use-case specific
  
  # API Gateway WAF - Optional security feature, adds cost
  - CKV2_AWS_29  # WAF protection adds significant cost and complexity
